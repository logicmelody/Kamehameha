import {
	StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
	container: {
		flex: 1,
		justifyContent: 'center',
		alignItems: 'center',
	},

	kamehamehaImage: {
		height: 300,
		marginBottom: 20,
	},

	vegetaImage: {
		position: 'absolute',
		left: 0,
		top: 0,
		width: 300,
	},
});

export default styles;
